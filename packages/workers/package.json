{
  "name": "@otak-jinro/workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers バックエンド",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@otak-jinro/shared": "file:../shared"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.16.1",
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
